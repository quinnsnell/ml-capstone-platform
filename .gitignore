# =============================================================================
# NEVER COMMIT
# =============================================================================

# Certificates & keys
_secrets/
star_cs_byu_edu*.zip
star_cs_byu_edu*/
*.pem
*.key
*.crt
*.pfx
*.p12
*-private-key*
*private-key*
MJ-test-key

# Environment files with real values
.env
.env.local
.env.production
coolify-env

# GitHub App private keys (extra explicit — any .pem ending)
ml-capstone-coolify*.pem
*-github-app*.pem

# Coolify state
coolify-*.tgz

# =============================================================================
# Build / cache junk
# =============================================================================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/
node_modules/
build/
dist/

# mdbook build output — regenerated by CI on every push
docs/book/

# =============================================================================
# Editor / OS
# =============================================================================
.DS_Store
*.swp
*.swo
.vscode/
.idea/
*~

# =============================================================================
# Nested repos (tracked separately on GitHub, not by this repo)
# =============================================================================
# Class app repos live at github.com/byu-ml-capstone/*. Keeping local clones
# here (alongside the platform code) is convenient for testing but each is its
# own git repo — exclude from this repo's tracking.
sentiment-test-app/
qsnell-hello-world-app/
# Match any student's hello-world clone by the naming convention students use
# (student-guide.md Step 4: <team-slug>-hello-world-app or similar).
*-hello-world-app/
hello-world-app/

# =============================================================================
# Local test artifacts
# =============================================================================
.claude/

# =============================================================================
# Ad-hoc scripts kept local
# =============================================================================
sendsimplemessage.py
